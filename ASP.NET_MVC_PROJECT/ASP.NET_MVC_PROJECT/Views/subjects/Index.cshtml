@model IEnumerable<ASP.NET_MVC_PROJECT.Models.submodel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.subjects_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.subjects_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.subjects_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.subjects_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.subjects_id }) |
            @Html.ActionLink("Details", "Details", new { id = item.subjects_id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.subjects_id })
        </td>
    </tr>
}

</table>
