@model IEnumerable<ASP.NET_MVC_PROJECT.Models.classmodel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.class_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.class_strength)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.class_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.class_strength)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.class_id  }) |
            @Html.ActionLink("Details", "Details", new {  id=item.class_id  }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.class_id })
        </td>
    </tr>
}

</table>
